# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
wheels/

# --- uv / virtualenv ---
.venv/
venv/
env/
# uv.lock is committed intentionally (reproducible installs) — do not ignore it.

# --- Testing / coverage ---
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.cache/

# --- Type checkers / linters ---
.mypy_cache/
.ruff_cache/
.dmypy.json

# --- Jupyter ---
.ipynb_checkpoints/

# --- skin-metrics specific ---
# Downloaded MediaPipe FaceLandmarker model (~3.8MB, fetched at runtime)
*.task
# Input face photos and generated reports (user data / outputs)
data/*
!data/.gitkeep
report*.json
*.report.json

# --- Calibration corpus (AI-Hub 028 한국인 피부상태 측정 데이터, ~43GB) ---
# Redistribution-restricted source data. Only the *fitted* parameters that land
# in skin_metrics/config.yaml are committed.
028.한국인 피부상태 측정 데이터/
한국인 피부상태 측정 데이터/
calibration_work*/

# --- ML artifacts (Phase 2) ---
*.pt
*.pth
*.ckpt
runs/
checkpoints/
lightning_logs/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~
